%function [normalForce, cpLoc] = normalForce(alpha, rho, v)

degToRad = 2*3.14159 / 360;

%[normalForce, cpLoc] = normalForce (1*degToRad, 0.0501,738.56);
%disp (cpLoc);

%1.1924 * 10 ^4

test = 16;

sign = abs (test) / test;
disp (sign);